DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/recruitfast
JWT_SECRET=replace-with-a-long-random-value
# Optional — CV parsing falls back to the rule-based parser if unset.
LLM_API_KEY=
LLM_BASE_URL=https://ai.sumopod.com/v1
LLM_MODEL=gemini/gemini-3.1-flash-lite
